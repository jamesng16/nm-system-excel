---
wave: 1
depends_on: []
files_modified:
  - supabase/config.toml
  - supabase/migrations/20260520000000_init_schema.sql
  - docker-compose.yml
autonomous: true
requirements:
  - INF-01
  - INF-02
---

# Kế hoạch 01-01: Thiết lập Local Supabase & SQL Migrations

Kế hoạch này tập trung vào việc thiết lập môi trường backend Supabase cục bộ sử dụng CLI và Docker. Chúng ta sẽ viết các script SQL migration thô để tạo cấu trúc cơ sở dữ liệu (các bảng `files`, `excel_data_rows`, `excel_row_images`, `processing_logs`) cùng các chỉ mục tối ưu cần thiết để xử lý tệp Excel lớn chứa hình ảnh trích xuất.

## Các tác vụ (Tasks)

<task>
  <subject>Khởi tạo cấu hình Supabase CLI cục bộ</subject>
  <description>Tạo file cấu hình config.toml cho Supabase local và cấu hình các giá trị mặc định cho database cùng storage.</description>
  <read_first>
    - .planning/phases/01-local-supabase-db-migrations/01-CONTEXT.md
    - .planning/phases/01-local-supabase-db-migrations/01-RESEARCH.md
  </read_first>
  <action>
    - Khởi tạo dự án Supabase cục bộ.
    - Cấu hình file `supabase/config.toml` đảm bảo cấu hình đúng các cổng kết nối local (API: 54321, DB: 54322, Studio: 54323).
  </action>
  <acceptance_criteria>
    - File `supabase/config.toml` được tạo thành công.
    - Chạy lệnh `supabase status` hiển thị thông tin cấu hình hợp lệ.
  </acceptance_criteria>
</task>

<task>
  <subject>Viết SQL migration khởi tạo database schema</subject>
  <description>Viết script SQL migration thô định nghĩa cấu trúc các bảng files, excel_data_rows, excel_row_images và processing_logs kèm theo các ràng buộc khóa ngoại và index.</description>
  <read_first>
    - .planning/phases/01-local-supabase-db-migrations/01-RESEARCH.md
  </read_first>
  <action>
    - Tạo file SQL migration mới tại: `supabase/migrations/20260520000000_init_schema.sql`.
    - Viết định nghĩa tạo các bảng `files`, `excel_data_rows`, `excel_row_images` và `processing_logs` trong file SQL.
    - Thiết lập các ràng buộc khóa ngoại liên kết kèm cơ chế ON DELETE CASCADE.
    - Tạo GIN index trên cột `excel_data_rows.content` và các index chuẩn trên các khóa ngoại.
  </action>
  <acceptance_criteria>
    - File SQL migration `supabase/migrations/20260520000000_init_schema.sql` được tạo đúng vị trí và chứa cấu trúc bảng hợp lệ.
    - Áp dụng migration bằng lệnh `supabase db reset` thành công mà không có lỗi SQL.
  </acceptance_criteria>
</task>

<task>
  <subject>Tạo cấu hình docker-compose.yml dự phòng</subject>
  <description>Cung cấp file docker-compose.yml tại thư mục gốc để khởi chạy Postgres và Studio trực tiếp mà không bắt buộc cài đặt Supabase CLI toàn cục.</description>
  <read_first>
    - .planning/phases/01-local-supabase-db-migrations/01-RESEARCH.md
  </read_first>
  <action>
    - Viết file `docker-compose.yml` chứa PostgreSQL 15, PostgREST và Studio container để hỗ trợ chạy môi trường dev độc lập.
  </action>
  <acceptance_criteria>
    - File `docker-compose.yml` tồn tại ở thư mục gốc và chứa định nghĩa cấu hình Docker hợp lệ.
  </acceptance_criteria>
</task>

## Xác thực (Verification)

### Must Haves
- [ ] Chạy stack Supabase cục bộ thành công và truy cập được giao diện quản lý Studio.
- [ ] Cấu trúc database schema được khởi tạo đầy đủ cùng các chỉ mục hiệu năng cao.
