---
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tailwind.config.js
  - src/main.tsx
  - src/index.css
  - src/modules/auth/domain/entities/User.ts
  - src/modules/auth/domain/repositories/IAuthRepository.ts
  - src/modules/auth/usecases/LoginUser.ts
  - src/modules/auth/adapters/SupabaseAuthRepository.ts
  - src/modules/auth/presentation/hooks/useAuth.ts
  - src/modules/auth/presentation/views/LoginView.tsx
  - src/modules/excel-processor/domain/entities/ExcelFile.ts
  - src/modules/excel-processor/domain/entities/ExcelRow.ts
  - src/modules/excel-processor/domain/repositories/IExcelRepository.ts
  - src/modules/excel-processor/usecases/UploadExcel.ts
  - src/modules/excel-processor/usecases/GetIngestedRows.ts
  - src/modules/excel-processor/adapters/SupabaseExcelRepository.ts
  - src/modules/excel-processor/presentation/hooks/useExcelProcessor.ts
  - src/modules/excel-processor/presentation/views/DashboardView.tsx
autonomous: true
requirements:
  - INF-03
---

# Kế hoạch 01-02: Bootstrap Frontend & Scaffold các Module Clean Architecture

Kế hoạch này tập trung vào việc khởi tạo dự án frontend React Vite TSX sử dụng Tailwind CSS và thiết lập bộ khung cấu trúc Clean Architecture theo từng Module độc lập cho hai tính năng: `auth` và `excel-processor`. Đảm bảo cô lập hoàn toàn logic nghiệp vụ cốt lõi khỏi các thư viện hoặc framework bên ngoài.

## Các tác vụ (Tasks)

<task>
  <subject>Bootstrap dự án React Vite + TSX</subject>
  <description>Khởi tạo một dự án React Vite TSX mới, thiết lập cấu hình biên dịch và tích hợp Tailwind CSS để thiết kế UI.</description>
  <read_first>
    - .planning/phases/01-local-supabase-db-migrations/01-CONTEXT.md
  </read_first>
  <action>
    - Tạo file package.json chứa các dependency: react, react-dom, @supabase/supabase-js, tailwindcss, postcss, autoprefixer, lucide-react.
    - Cấu hình file `vite.config.ts` và tsconfig compiler settings.
    - Thiết lập file cấu hình Tailwind `tailwind.config.js` và file style đầu vào `src/index.css`.
  </action>
  <acceptance_criteria>
    - Script compile Vite và cấu hình Tailwind tồn tại đầy đủ.
    - Directives của Tailwind tồn tại trong file `src/index.css`.
  </acceptance_criteria>
</task>

<task>
  <subject>Scaffold thư mục và cấu trúc cốt lõi cho module Auth</subject>
  <description>Tạo cấu trúc Clean Architecture bên trong thư mục module Auth, viết các thực thể (entities) và interface repository thuần túy.</description>
  <read_first>
    - .planning/phases/01-local-supabase-db-migrations/01-RESEARCH.md
  </read_first>
  <action>
    - Tạo cây thư mục `src/modules/auth` chứa: `domain/entities/`, `domain/repositories/`, `usecases/`, `adapters/`, `presentation/views/`, `presentation/components/`, `presentation/hooks/`.
    - Viết thực thể domain thuần túy `User.ts` (id, email, role).
    - Viết interface abstract repository `IAuthRepository.ts` chứa các hàm login/logout.
    - Implement logic usecase nghiệp vụ `LoginUser.ts`.
  </action>
  <acceptance_criteria>
    - Các file trong tầng Domain không import bất kỳ package nào từ React hay Supabase Client.
    - Cây thư mục module Auth được tạo đầy đủ.
  </acceptance_criteria>
</task>

<task>
  <subject>Scaffold thư mục và cấu trúc cốt lõi cho module Excel Processor</subject>
  <description>Tạo cấu trúc Clean Architecture bên trong thư mục module Excel Processor, viết thực thể domain và interface repository.</description>
  <read_first>
    - .planning/phases/01-local-supabase-db-migrations/01-RESEARCH.md
  </read_first>
  <action>
    - Tạo cây thư mục `src/modules/excel-processor` chứa các thư mục con: `domain/entities/`, `domain/repositories/`, `usecases/`, `adapters/`, `presentation/views/`, `presentation/components/`, `presentation/hooks/`.
    - Viết thực thể domain `ExcelFile.ts` (id, name, size, status, totalRows, processedRows) và `ExcelRow.ts` (id, fileId, rowIndex, content).
    - Viết interface abstract repository `IExcelRepository.ts` định nghĩa các hàm upload, lấy danh sách dòng dữ liệu, theo dõi tiến độ.
    - Implement logic usecases nghiệp vụ `UploadExcel.ts` và `GetIngestedRows.ts`.
  </action>
  <acceptance_criteria>
    - Tầng Domain của Excel Processor độc lập hoàn toàn khỏi React hay Supabase SDK.
    - Thư mục module được cấu trúc chuẩn chỉnh.
  </acceptance_criteria>
</task>

<task>
  <subject>Xây dựng các adapter cơ bản, custom hooks và Mock UI</subject>
  <description>Viết khung các lớp adapter tích hợp với Supabase client và giao diện dashboard mock ban đầu để kiểm tra ứng dụng compile thành công.</description>
  <read_first>
    - .planning/phases/01-local-supabase-db-migrations/01-CONTEXT.md
  </read_first>
  <action>
    - Tạo file khởi tạo supabase client dùng chung: `src/shared/infra/supabase.ts`.
    - Viết các adapter shell `SupabaseAuthRepository` và `SupabaseExcelRepository` trả về dữ liệu mock tạm thời.
    - Tạo custom hooks `useAuth` và `useExcelProcessor` làm cầu nối giữa UI components và Use Cases.
    - Viết view mock đơn giản `LoginView.tsx` và `DashboardView.tsx` để điều hướng trang.
  </action>
  <acceptance_criteria>
    - Custom hooks liên kết chính xác các UI component với domain logic usecases.
    - Chạy lệnh Vite build không có bất kỳ lỗi TypeScript hay compile nào.
  </acceptance_criteria>
</task>

## Xác thực (Verification)

### Must Haves
- [ ] Các thư mục module con bên trong `src/modules/` được phân tách Clean Architecture đúng quy chuẩn.
- [ ] Ứng dụng React, Vite, TSX, và Tailwind CSS khởi động và chạy thử bình thường mà không có lỗi compile.
